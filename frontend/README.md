# ChatBuff 前端项目

基于 React + Vite + Tailwind CSS 的极简赛博朋克风格 UI。

## 项目结构

```
frontend/
├── src/
│   ├── App.jsx           # 主应用组件
│   ├── index.css         # 全局样式
│   └── main.jsx          # 应用入口
├── index.html            # HTML 模板
├── vite.config.js        # Vite 配置
├── tailwind.config.js    # Tailwind CSS 配置
├── postcss.config.js     # PostCSS 配置
└── package.json          # 项目依赖
```

## 设计特点

### 🎨 视觉风格
- **主题**：极简赛博朋克 (Minimalist Cyberpunk)
- **背景**：深色背景（Slate-950）
- **配色**：青色 (Cyan) + 紫色 (Violet) 渐变
- **质感**：玻璃拟态 (Glassmorphism) + 半透明背景 + 背景模糊

### 💻 技术栈
- **框架**：React 18 + Vite
- **样式**：Tailwind CSS 3
- **动画**：Framer Motion
- **图标**：Lucide React

### ✨ 特性
- 响应式设计（移动端友好）
- 光晕效果和渐变动画
- 平滑的过渡和交互反馈
- 代码/终端风格的字体 (Monospace)
- 自定义滚动条样式

## 快速开始

### 1. 安装依赖

如果你已安装 Node.js 和 npm：

```bash
# 在 frontend 目录下
npm install
```

### 2. 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:3000` 即可查看应用。

### 3. 构建生产版本

```bash
npm run build
```

生成的静态文件在 `dist/` 目录下。

## API 集成

前端已配置代理，所有 `/api/*` 的请求都会被转发到后端：

```javascript
// vite.config.js 中的代理配置
proxy: {
  '/api': {
    target: 'http://127.0.0.1:8000',
    changeOrigin: true
  }
}
```

## 核心组件说明

### App.jsx

主应用组件，包含：

- **输入区**：textarea 用于输入用户想说的话
- **操作区**：获取建议按钮 + 语音输入按钮
- **结果区**：展示建议和相关金句
- **空状态**：引导用户输入

### 样式系统

自定义 CSS 类：

```css
.glass          /* 玻璃拟态基础 */
.glass-sm       /* 较小的玻璃拟态 */
.gradient-text  /* 渐变文字 */
.btn-primary    /* 主按钮 */
.btn-secondary  /* 次级按钮 */
.input-primary  /* 输入框 */
```

## 后续开发

- [ ] 实现语音输入功能（Web Speech API）
- [ ] 添加用户历史记录
- [ ] 收藏夹功能
- [ ] 暗/亮模式切换
- [ ] PWA 支持

## 常见问题

**Q: npm install 失败？**

A: 确保已安装 Node.js 18+。如果持续失败，尝试：
```bash
npm cache clean --force
npm install
```

**Q: 前端连不上后端？**

A: 确保后端服务运行在 `http://127.0.0.1:8000`，且 CORS 已启用。

**Q: 样式没有应用？**

A: 确保 Tailwind CSS 的 content 配置包含了所有 src 文件。

## 自定义配置

### 修改主题色

编辑 `tailwind.config.js`：

```javascript
theme: {
  extend: {
    colors: {
      // 修改这里
      cyan: { 500: '#06b6d4' },
      violet: { 500: '#a855f7' }
    }
  }
}
```

### 修改背景动画

编辑 `src/App.jsx` 中的动态背景部分：

```jsx
<div className="absolute top-0 left-1/4 w-96 h-96 bg-cyan-500/10 rounded-full blur-3xl animate-float"></div>
```

## License

MIT
